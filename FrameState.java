package brainstorm;

/** FrameState controls what frames should be active depending on where the player is */

public enum FrameState
{
    /**Map is the state when player is in map*/
    MAP,
    /**Arena is the state when player is in arena*/
    ARENA
}
